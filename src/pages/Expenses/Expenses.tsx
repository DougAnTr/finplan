import React, { FunctionComponent } from 'react';

const Expenses: FunctionComponent = () => {
    return (
        <div>
            <h2>Despesas</h2>
        </div>
    );
};

export default Expenses;
